({
	handleActive : function(component, event) {
         var tab = event.getSource();
        switch (tab.get('v.id')) {
            case 'tab1':
                break;
            case 'tab2':
                break;
        }
	}
})